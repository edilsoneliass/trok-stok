import styled from 'styled-components';

export const Title = styled.h1`
    font-size: 12px;
    color: white;
    text-align: left;
    text-decoration: underline;
    :hover {
        cursor: pointer;
        color: #e0e0e0;
    }
`;
